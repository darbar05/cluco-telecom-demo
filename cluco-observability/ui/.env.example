# Optional: full URL to Cluco backend when not using Vite proxy (e.g. production build or backend on another host).
# Leave unset in dev so the UI uses relative /api/v1 and the Vite proxy (vite.config.js) forwards to http://localhost:9410.
# VITE_API_URL=http://localhost:9410/api/v1
